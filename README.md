# About
A program that uses the MNIST database to recognise digits with a neural network.

# Build

Create a build folder
```
```
cd path/to/digit-identifier
mkdir build
```
```

Run cmake and make from inside the build folder
```
```
cd build
cmake ..
make
```
```

